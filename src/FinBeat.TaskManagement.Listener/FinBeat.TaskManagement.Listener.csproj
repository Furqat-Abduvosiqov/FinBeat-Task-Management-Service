<Project Sdk="Microsoft.NET.Sdk.Worker">

    <PropertyGroup>
        <UserSecretsId>dotnet-FinBeat.TaskManagement.Listener-9eb89a56-3981-4e6c-a7ad-a1b3ed2ae132</UserSecretsId>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.Extensions.Hosting"/>
    </ItemGroup>

    <!--
        Contracts and nothing else. This is a separate deployable, so the wire format is all it may
        share with the API - give it Application and it becomes the same monolith deployed twice.
    -->
    <ItemGroup>
        <ProjectReference Include="..\FinBeat.TaskManagement.Contracts\FinBeat.TaskManagement.Contracts.csproj" />
    </ItemGroup>
</Project>
